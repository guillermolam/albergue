#!/bin/sh
# Husky commit-msg hook – OPA policy gate for commit message quality.
set -e

if [ -x "security/policies/run-opa-gate.sh" ]; then
	security/policies/run-opa-gate.sh commit-msg "$1" || exit 1
fi
