# This workflow uses actions that are not certified by GitHub.
# They are provided by a third-party and are governed by
# separate terms of service, privacy policy, and support
# documentation.

name: trivy

on:
  push:
    branches: ["main"]
  pull_request:
    # The branches below must be a subset of the branches above
    branches: ["main"]
  schedule:
    - cron: "45 11 * * 2"

permissions: {}

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    permissions:
      contents: read # for actions/checkout to fetch code
      security-events: write # for github/codeql-action/upload-sarif to upload SARIF results
      actions: read # only required for a private repository by github/codeql-action/upload-sarif to get the Action run status
    name: Build
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      # This app currently has no containerized service (no Dockerfile in
      # the repo), so there is nothing for Trivy to build or scan yet. The
      # remaining steps activate automatically once a Dockerfile is added.
      - name: Check for Dockerfile
        id: dockerfile
        run: echo "present=$([ -f Dockerfile ] && echo true || echo false)" >>"$GITHUB_OUTPUT"

      - name: Build an image from Dockerfile
        if: steps.dockerfile.outputs.present == 'true'
        env:
          IMAGE_TAG: docker.io/my-organization/my-app:${{ github.sha }}
        run: |
          docker build -t "$IMAGE_TAG" .

      - name: Run Trivy vulnerability scanner
        if: steps.dockerfile.outputs.present == 'true'
        uses: aquasecurity/trivy-action@7b7aa264d83dc58691451798b4d117d53d21edfe
        with:
          image-ref: "docker.io/my-organization/my-app:${{ github.sha }}"
          format: "template"
          template: "@/contrib/sarif.tpl"
          output: "trivy-results.sarif"
          severity: "CRITICAL,HIGH"

      - name: Upload Trivy scan results to GitHub Security tab
        if: steps.dockerfile.outputs.present == 'true'
        uses: github/codeql-action/upload-sarif@3ea06614dafe36dec890db3446326e0d40ce53d4 # v3.31.6
        with:
          sarif_file: "trivy-results.sarif"
